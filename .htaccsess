ErrorDocument 404 /404.html

Redirect 301 /discord https://discord.gg/jMXr2Cp7eF

RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^([^\.]+)$ $1.html [NC,L]

# Redirect /quoted/ to /Quoted/
RewriteRule ^quoted/$ /Quoted/ [R=301,L]

# Redirect /quoted to /Quoted
RewriteRule ^quoted$ /Quoted [R=301,L]
